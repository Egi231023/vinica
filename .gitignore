node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store

# Projected map geometry — regenerated by `npm run map`, which `dev`,
# `typecheck` and `build` all run first.
src/data/generated/
